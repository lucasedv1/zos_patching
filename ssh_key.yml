---
- name: Send ssh key
  hosts: all
  collections:
    - ibm.ibm_zos_core
  gather_facts: no
  environment: "{{ environment_vars }}"
  
  tasks:
    - name: Send ssh key
      file:
        path: type /Users/lucasvasconcelos/authorized_keys.txt "cat >> .ssh/authorized_keys"
        state: directory
